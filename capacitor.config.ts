import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-5-mojix-starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
